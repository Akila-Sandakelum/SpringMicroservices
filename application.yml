---
server:
  port: 8010
eureka:
  instance:
    hostname: localhost
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
    register-with-eureka: false
    fetch-registry: false
  
